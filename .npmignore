# Prefer package.json "files" as the source of truth.
# Keep this file so npm does not fall back to .gitignore (which excludes dist/).
* 
!dist/
!dist/**
!translations/
!translations/**
!package.json
!README.md
!LICENSE
